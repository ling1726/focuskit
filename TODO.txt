- handle events in order
- make creating event easier -> shouldn't define base things all the time
- update commands in after each other to array of commands
- handle default focused element
- short circuit based on isClosestEntity
- List: handle RTL -> use pipe state
- Connect Traps
- handle shift tab in tests;
